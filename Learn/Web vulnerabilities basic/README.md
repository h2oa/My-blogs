# SQL injection

Q: What is SQL injection?

A: When user's input is not validate and directly execute in a SQL query -> attacker can modify the inject payload to retrieve data from database, such as user's PII data, password, ... or attacker can do anything like he can directly execute the SQL query with database.

